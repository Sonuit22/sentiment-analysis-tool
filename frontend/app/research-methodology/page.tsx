import { PageIntro } from "@/components/UI";
export const metadata={title:"Research methodology"};
const steps=[["Data preparation","Remove missing text/label rows, normalize text values, harmonize labels, and validate at least two examples per class."],["Stratified split","Create a held-out test partition using the configured test size and random seed while preserving class ratios."],["Independent training","Fit each selected model through its original module-level train function; no algorithm or prediction rule is rewritten."],["Evaluation","Measure accuracy, weighted precision, weighted recall, weighted F1, training time, confusion matrix, class report, and sample classifications."],["Interpretation","Compare scores alongside class distribution, word clouds, keyword frequency, and domain limitations."],["Live analysis","Use the fitted pipeline for text or transcribed WAV inference and return model provenance plus timing."]];
export default function ResearchPage(){return <div className="page-wrap"><PageIntro kicker="Reproducible study design" title="Research methodology" description="A transparent end-to-end protocol for preparing data, evaluating classical models, and interpreting results without changing model behavior."/>
<div className="timeline">{steps.map(([title,description],index)=><div className="timeline-item" key={title}><div className="timeline-index">{index+1}</div><div className="card"><h3>{title}</h3><p>{description}</p></div></div>)}</div>
<div className="grid grid-2 section"><section className="card"><h2>Controls</h2><ul className="muted"><li>Configurable 15?40% test split</li><li>Configurable deterministic random seed</li><li>Stratification by sentiment label</li><li>Selectable model subset</li><li>Zero-division-safe classification metrics</li></ul></section><section className="card"><h2>Known limitations</h2><ul className="muted"><li>Sarcasm and highly contextual language remain difficult.</li><li>Scores depend on dataset quality and domain similarity.</li><li>Confidence is model-derived, not a guarantee of correctness.</li><li>Speech recognition depends on an external service.</li><li>Serverless instances may cold-start and rebuild the deterministic in-memory baseline.</li></ul></section></div></div>;}
